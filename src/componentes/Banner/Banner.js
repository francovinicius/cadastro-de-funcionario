import './Banner.css'

export const Banner = () => {
    // JSX
    return (
            //no react usamos calssName e não class
        <header className='banner'> 
            <img src="/imagens/banner.png" alt="O banner principal da página" />
        </header>
        
    )
};